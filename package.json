{
  "name": "chat",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@sap/cds": "^9",
    "better-sqlite3": "^12.4.1",
    "dotenv": "^17.2.3",
    "express": "^4",
    "sqlite-vec": "^0.1.7-alpha.2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9.4.3"
  },
  "scripts": {
    "start": "cds-serve",
    "watch-project1": "cds watch --open project1/webapp/index.html?sap-ui-xx-viewCache=false",
    "init:vectors": "node tools/init-vectors.js"
  },
  "private": true,
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "sqlite.db"
        }
      }
    }
  }
}
